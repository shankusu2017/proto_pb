syntax = "proto3";

package node;

option go_package = "/;proto";

enum Role {
  Default = 0;
  Pac = 1;
  Repeater = 1000;
}

message Node {
  string Ver = 1;   /* 版本 */
  Role role = 2;    /* 角色 */
}